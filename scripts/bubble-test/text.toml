# Regression baseline: nothing configured, nothing changed.
# SCENARIO=text (or any other) must arrive as ONE ordinary message.
[agent]
command = "python3"
args = ["scripts/fake-acp-agent.py"]
# The agent is spawned with this as its cwd; "." = wherever you ran openab from,
# which must be the repo root for the relative path above to resolve.
working_dir = "."
env = { SCENARIO = "text" }

[gateway]
url = "ws://127.0.0.1:8765"
platform = "telegram"
allow_all_users = true
allow_all_channels = true
streaming = false

[reactions]
enabled = false
